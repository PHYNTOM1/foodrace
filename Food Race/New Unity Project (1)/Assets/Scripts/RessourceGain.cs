using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class RessourceGain : MonoBehaviour
{
    /*
    int resource;
    int maxResource;

    public Slider resourceBar;
    public Text currentResource;

    private CartController cc;

    void Start()
    {
        resource = 0;
        maxResource = 30;
        cc = this.gameObject.GetComponent<CartController>();

        resourceBar.value = resource;
        resourceBar.maxValue = maxResource;

        currentResource.text = "Resource : 0";


    }


    void Update()
    {
        if (Input.GetKeyDown(KeyCode.W))
        {
            resource += 1;
        }

        if (cc.driftStage == 1)
        {
            resource += 2;
        }
        else if (cc.driftStage == 2)
        {
            resource += 3;
        }

        resourceBar.value = resource;
    }
    */
}